# 撤稿索引 SQLite 默认路径（相对于项目根或绝对路径）
default_index_path: reference_anomaly_detection/config/retraction_watch_index.sqlite

# 无 DOI 时题名模糊匹配（rapidfuzz token_set_ratio，0–1）
retraction_title_match_threshold: 0.92
retraction_title_review_threshold: 0.85
fts_top_k: 20
